# 无障碍字幕时轴校对工作台

## 目标用户与业务闭环

字幕质检员针对一段音视频创建媒体记录、登记说话人并导入带序号和绝对时间窗的转写片段。服务从片段内容、阅读速度、时间关系和说话人归属派生质量项；编辑者以片段版本号校对内容或时轴。质检员发布冻结的字幕快照，后续修改生成新版本，旧版本只可撤回与比较。

完整闭环为：媒体登记 → 说话人登记 → 批量导入字幕 → 质量检查 → 乐观并发校对 → 发布快照 → 撤回或比较版本 → 重启后恢复质量项。

## 实体、状态与不变量

- Media：draft、ready、published、archived；保存媒体语言、时长、帧率和来源信息。
- Segment：draft、proofed、conflict、published；每段有 index、start_ms、end_ms、文本、说话人、描述性标记和 version。
- QualityCheck：保存重叠、空洞、超速、空行、过长行、描述缺失和未知说话人等派生结果。
- Revision 与 Conflict：保存编辑前后快照、操作者和因 base_version 陈旧而被拒绝的并发编辑。
- PublishVersion 与 Withdrawal：发布时保存冻结 JSON 快照；撤回只追加记录并置标记，不能改写快照。

不变量：片段结束时间必须晚于开始时间；同一媒体的序号不可重复；陈旧版本编辑不得覆盖当前内容；发布后的版本必须能独立读取并与后续版本进行字段级比较。

## 持久化与恢复

SQLite 保存 media、speakers、segments、revisions、quality_checks、publish_versions、withdrawals 和 conflicts。启动时 Service.Recover 对所有非归档媒体幂等重算质量项，不触碰已冻结发布快照。--smoke-test 创建数据、关闭并重开数据库，验证恢复后的媒体、片段、冲突和版本仍可读取。

## API 与页面入口

cmd/subtitleqa 启动真实 HTTP 服务；internal/httpapi 在 /api 下暴露媒体、说话人、片段导入/编辑、质量查询/重算、发布/撤回/比较、冲突和自检接口。根路径页面由 internal/webui 提供，只通过这些 API 显示片段、质量项、版本和冲突，并提交实际编辑与发布请求。

## 并发与错误边界

编辑请求必须携带 base_version。服务层在写入前比较当前 Segment.Version：匹配时写入修订并重算质量项；不匹配时写入 Conflict 并返回冲突错误，绝不静默合并。导入拒绝空批次、负序号、重复序号、负开始时间和非正时长；归档媒体拒绝继续导入或发布。

## 模块责任

- store：SQLite schema、实体持久化和恢复读取。
- timeline：纯时轴规则与阅读速度判定。
- qa：把时轴规则编排为持久化质量项和摘要。
- revision：从编辑请求生成带前后快照的修订。
- publish：构建、解析、比较冻结版本。
- service：业务边界、并发门禁和跨模块编排。
- httpapi、webui、demo、metrics：真实服务入口、页面、端到端自检和运行计数。

## 端到端验收场景

1. HTTP 客户端创建媒体并导入空字幕和空洞片段，读取质量摘要时得到对应质量项。
2. 两位编辑者基于同一版本编辑同一片段；首个编辑成功，第二个编辑被拒绝且保存冲突记录。
3. 发布 v1 后编辑字幕并发布 v2；版本比较能报告字段变化，读取 v1 仍得到原冻结快照。
4. 服务关闭和重开后，非归档媒体重新得到一致质量项，已发布/撤回版本和冲突记录仍保留。
